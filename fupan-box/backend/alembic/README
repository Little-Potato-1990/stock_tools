Alembic migrations (async, postgresql+asyncpg)
==============================================

数据库 schema 由 alembic 管理. main.py lifespan 默认不再调用
``Base.metadata.create_all``, 全部走迁移.

常用命令 (在 backend/ 目录下执行):

  # 应用所有未执行的迁移
  alembic upgrade head

  # 看当前数据库停在哪个版本
  alembic current

  # 看历史
  alembic history

  # 改了 model 之后, 让 alembic 对比 model 与 DB, 生成增量迁移
  alembic revision --autogenerate -m "add some_table.some_col"

  # 紧急回滚一步 (慎用)
  alembic downgrade -1

注意:
- alembic.ini 里的 sqlalchemy.url 是占位符. 真正的 URL 由 alembic/env.py
  从 ``app.config.get_settings().database_url`` 注入, 与运行时一致.
- 全新部署: 创建空数据库, 然后 ``alembic upgrade head`` 即可.
- 既有库 (alembic 接管前): 先 ``alembic stamp 38c36c7b12df`` 标记 baseline,
  再 ``alembic upgrade head``.
- 仅本地紧急情况下可设环境变量 DEV_AUTO_CREATE_ALL=1, 让 lifespan 重新执行
  Base.metadata.create_all (不会自动 stamp, 仅救急).
